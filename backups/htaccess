# Bloquea por completo el acceso HTTP a esta carpeta: los backups son solo para uso interno del servidor
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
